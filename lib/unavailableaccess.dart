import 'package:flutter/material.dart';

class UnavailableAccessPage extends StatelessWidget {
  const UnavailableAccessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Unavailable Access");
  }
}
